---
applications:
- name: nsf-demo
  buildpack: php_buildpack
  instances: 1
  memory: 512M
  disk_quota: 512M
  timeout: 180
  services:
    - database # cf create-service aws-rds medium-mysql database
    - secrets  # cf create-user-provided-service secrets -p '{"ROOT_USER_NAME": ..., "ROOT_USER_PASS": ..., "HASH_SALT": ...}'
    - storage  # cf create-service s3 basic storage
